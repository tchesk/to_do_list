<h1> Lista de tarefas </h1>
<ul>
  <% @lists.each do |list| %>
    <li><%= list.title %></li>
  <% end %>
</ul>

<% @lists.each do |list| %>
  <%= link_to list.title, task_list_path(list.id) %>
<% end %>
